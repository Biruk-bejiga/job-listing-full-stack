/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // `appDir` is stable in recent Next.js versions; remove experimental key
  // to avoid the invalid-next-config warning in dev.
};
module.exports = nextConfig;
